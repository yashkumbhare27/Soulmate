# Local dev: point to your local backend
NEXT_PUBLIC_API_URL=http://localhost:5000

# Production (set this in Vercel project settings instead of committing it):
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
